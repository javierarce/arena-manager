tag-version-prefix=""
; vitest's optional peer wants @types/node >=20, but the plugin pins 16 for the
; Obsidian/Electron runtime. Accept that conflict instead of failing installs.
legacy-peer-deps=true